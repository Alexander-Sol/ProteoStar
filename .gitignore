# --- Rust ---
# ProteoStar ships binaries (Tauri app + py extension), so Cargo.lock IS committed
# (it carries the load-bearing thermorawfilereader = 0.7.0 pin). Do not ignore it.
target/
**/*.rs.bk

# --- Node / TypeScript / Vite (desktop frontend + packages) ---
node_modules/
dist/
build/
coverage/
.next/
out/
.vercel/
.env
.env.local
.env.*.local
*.log

# --- Python (smoke-test venv + maturin artifacts) ---
.venv/
**/*.egg-info/
**/__pycache__/
**/*.pyc
**/xic_smoke_plot.png

# --- C# golden generator build output (parity/csharp_golden) ---
[Bb]in/
[Oo]bj/
.vs/
*.user

# --- Automation / agent tooling ---
.ralph-logs/
.claude/
.serena/
.pr_comments/
AGENTS.md

# --- Analysis scratch data (large text/tsv outputs, not source) ---
analysis/**/*.txt
analysis/**/*.tsv
analysis/**/*.png
analysis/**/*.md

# --- OS / editor ---
Thumbs.db
.DS_Store
