# Task 001: Scaffold Workspace

## Goal

Create the initial Bun workspace and package layout for the web app and shared packages.

## Scope

1. Add root workspace configuration.
2. Create `apps/web`.
3. Create `packages/imsp-core`.
4. Create `packages/viewer-state`.
5. Create `packages/plot-adapter`.
6. Create `packages/ui`.
7. Add TypeScript and test tooling configuration.

## Deliverables

1. Bootable project structure
2. Shared TypeScript configuration
3. Package manifests wired together
4. Initial test command working

## Notes

Keep the initial scaffold minimal. Avoid overdesigning shared utilities before the parser and viewer state exist.
